<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>James' Awesome Tech Blog</title>
</head>
<body>
    <header>
        <h1>James' Awesome Tech Blog!</h1>
        <nav class="navigation">
            <a href="/">Home</a>
            {{#if logged_in}}
            <a href="/new-post">New Post</a>
            <a href="#" id="logout" class="last">Logout</a>
            {{else}}
            <a href="/login" class="last">Login</a>
            {{/if}}
        </nav>
    </header>
    
    <main>
        {{{ body }}}
    </main>

    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>
</html>